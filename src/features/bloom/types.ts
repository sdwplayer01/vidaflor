// src/features/bloom/types.ts

export interface BloomBreakdown {
  routinePct:    number;   // 0-100 (peso 40%)
  waterPct:      number;   // 0-100 (peso 30%)
  espiritualPct: number;   // 0-100 (peso 30%)
  total:         number;   // 0-100
}
